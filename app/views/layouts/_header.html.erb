<header class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">

      <%= link_to "PirckMarks", root_path, class: "brand", id: "logo" %>

        <% if signed_in? %>

          <ul class="nav">
            <!-- <li><%= link_to "使用者頁面", user_path(current_user) %></li> -->
          </ul>

          <ul class="nav pull-right">
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <img id="header-icon" src= <%= current_user.avatar %> > 
                 <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "使用者", user_path(current_user) %></li>
                <li><%= link_to "設定", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to "登出", signout_path, id: "sign_out" %></li>
              </ul>
            </li>
          </ul>

        <% else %>

          <ul class="nav pull-right">
            <li><%= link_to "用Facebook登入", "/auth/facebook", id: "sign_in" %></li>
            
          </ul>

        <% end %>


    </div>
  </div>
</header>